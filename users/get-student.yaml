  get:
    description: "sample get user for a given id"
    operationId: "getUserById"
    tags:
      - users
    parameters: 
      - $ref: "#/components/parameters/authorization"
    
    responses:
      "200":
        description: "student response"
        content: 
          application/json:
            schema:
              $ref: "#/components/schemas/get-ustudentResponse"

      "400": 
        $ref: "#/components/response/400"

          



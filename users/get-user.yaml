  get:
    description: "sample get user for a given id"
    operationId: "getUserById"
    tags:
      - users
    parameters: 
      - $ref: "#/components/parameters/authorization"
    
    responses:
      "200":
        description: "user response"
        content: 
          application/json:
            schema:
              -$ref:"#/components/schemas/get-userResponse"


